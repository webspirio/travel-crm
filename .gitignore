# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/settings.local.json

# DevContainer cache
.devcontainer/.cache/

# Local reference inputs (contain PII — never commit)
.input/

# Local reference repos (read-only study material — never commit)
.reference/

# Sibling project drafts (UA Well — separate repo, do not commit here)
.new-project/

# Environment files (contain anon/service-role keys per developer)
.env
.env.local
.env.*.local

# Supabase local state (per-developer)
supabase/.branches/
supabase/.temp/
supabase/seed.local.sql

# Generated DB types (regenerated via `npm run db:types`)
src/types/database.ts
src/types/database.ts.tmp
